name: CI

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:

concurrency:
  group:
    ${{ github.workflow }}-${{ github.ref_name }}-${{
    github.event.pull_request.number || github.sha }}
  cancel-in-progress: true

jobs:
  build:
    name: "build image"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and export
        uses: docker/build-push-action@v6
        with:
          tags: udemy-fastapi:latest
          cache-from: type=gha,scope=udemy-fastapi
          cache-to: type=gha,mode=min,scope=udemy-fastapi
          outputs: type=docker,dest=/tmp/udemy-fastapi.tar

      - name: Build and export (multistage)
        uses: docker/build-push-action@v6
        with:
          file: multistage.Dockerfile
          tags: udemy-fastapi-multistage:latest
          cache-from: type=gha,scope=udemy-fastapi-multistage
          cache-to: type=gha,mode=min,scope=udemy-fastapi-multistage
          outputs: type=docker,dest=/tmp/udemy-fastapi-multistage.tar

      - name: Build and export (standalone)
        uses: docker/build-push-action@v6
        with:
          file: standalone.Dockerfile
          tags: udemy-fastapi-standalone:latest
          cache-from: type=gha,scope=udemy-fastapi-standalone
          cache-to: type=gha,mode=min,scope=udemy-fastapi-standalone
          outputs: type=docker,dest=/tmp/udemy-fastapi-standalone.tar

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: udemy-fastapi
          path: /tmp/udemy-fastapi.tar

      - name: Upload artifact (multistage)
        uses: actions/upload-artifact@v4
        with:
          name: udemy-fastapi-multistage
          path: /tmp/udemy-fastapi-multistage.tar

      - name: Upload artifact (standalone)
        uses: actions/upload-artifact@v4
        with:
          name: udemy-fastapi-standalone
          path: /tmp/udemy-fastapi-standalone.tar

  test:
    name: "test image"
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4

      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: udemy-fastapi
          path: /tmp

      - name: Download artifact (multistage)
        uses: actions/download-artifact@v4
        with:
          name: udemy-fastapi-multistage
          path: /tmp

      - name: Download artifact (standalone)
        uses: actions/download-artifact@v4
        with:
          name: udemy-fastapi-standalone
          path: /tmp

      - name: Load images
        run: |
          docker load --input /tmp/udemy-fastapi.tar
          docker load --input /tmp/udemy-fastapi-multistage.tar
          docker load --input /tmp/udemy-fastapi-standalone.tar
          docker image ls -a

      - name: Test command line
        run: ./run.sh hello

      - name: Test Docker compose
        run: |
          docker compose up --watch -d
          docker compose down

      - name: Test command line (multistage)
        run: docker run udemy-fastapi-multistage:latest hello

      - name: Test command line (standalone)
        run: docker run udemy-fastapi-standalone:latest hello
